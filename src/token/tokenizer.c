#include <string.h>
#include "tokenizer.h"

void makeToken(const char* str, char* tokhold, bool* done)
{
    if(!(*done))
    {
        while(*str)
        {
            switch(*str)
            {
                case '>':
                    
                    break;
            }
        }
    }
}