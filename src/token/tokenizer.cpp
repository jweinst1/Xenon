#include <string>
#include <vector>
#include "../head/tokenizer.h"

std::vector<std::string> Tokenizer::tokenize(const char * input)
{
    std::vector<std::string> tokens = {"54", "->"};
    return tokens;
}